
    {{#each dataArtist}}
            {{#each images}}
                {{#if @first}}
                    <img src="{{ url }}" alt="image" />
                {{/if}}
            {{/each}}
            <h3> {{ name }} </h3>
            <a href="/albums/{{id}}"> View Albums</a>
            </br>
    {{/each}}
